{
  "name": "portal-series",
  "version": "1.0.0",
  "description": "Portal de séries com design moderno",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db/db.json"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}